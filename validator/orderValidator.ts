import { Order } from "../model/order";

module.exports.validateOrder = function (order: Order): string {
    // if(order.orderDate  > 50){
    //     return "Order can not be dated back to more than a year to the current date";
    // }
    
    return null;

}